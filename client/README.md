# x402 Rust client

Rust client library and example binaries for the x402 settlement program.

## Binaries

- `s1` - localnet e2e test
- `s2_http` - HTTP 402 handshake demo server and client
- `s3_relay` - open relayer market demo
